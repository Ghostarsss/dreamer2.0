spring:
  application:
    name: auth-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      # 配置中心
      config:
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
            group: DEFAULT_GROUP
            refresh: true
      # 服务注册发现
      discovery:
        server-addr: 127.0.0.1:8848